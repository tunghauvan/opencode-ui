services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./config/requirements.txt:/app/requirements.txt
      - ./.env:/app/.env
      - ./data:/app/data
      - opencode-sessions:/mnt/volume  # Shared workspace volume
    environment:
      - DATABASE_URL=sqlite:///./data/db.sqlite3
      - USE_MOCK=false
      - AGENT_CONTROLLER_URL=http://agent-controller:8001
      - AGENT_SERVICE_SECRET=default-secret-change-in-production
      - WORKSPACE_VOLUME_PATH=/mnt/volume
    env_file:
      - .env
    networks:
      - opencode-network

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://backend:8000
    networks:
      - opencode-network

  agent-controller:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    ports:
      - "8001:8001"
    volumes:
      - ./app:/app/app
      - ./config/requirements.txt:/app/requirements.txt
      - ./.env:/app/.env
      - ./data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DATABASE_URL=sqlite:///./data/db.sqlite3
      - AGENT_CONTROLLER_PORT=8001
      - AGENT_SERVICE_SECRET=default-secret-change-in-production
      - PYTHONPATH=/app
    env_file:
      - .env
    networks:
      - opencode-network
    command: ["python", "-m", "app.agent-controller"]

  idle-watcher:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    depends_on:
      - agent-controller
    volumes:
      - ./app:/app/app
      - ./scripts:/app/scripts
      - ./config/requirements.txt:/app/requirements.txt
      - ./.env:/app/.env
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/db.sqlite3
      - AGENT_CONTROLLER_URL=http://agent-controller:8001
      - AGENT_SERVICE_SECRET=default-secret-change-in-production
      - PYTHONPATH=/app
    command: ["python", "-u", "scripts/idle_watcher.py"]
    networks:
      - opencode-network

volumes:
  opencode-sessions:
    external: true  # Use the existing opencode-sessions volume

networks:
  opencode-network:
    driver: bridge